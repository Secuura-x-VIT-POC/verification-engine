You are grouping extracted document fields into verification-oriented credential candidates.

Task:
- group related fields into bounded credential candidates
- preserve the original extracted field ids as source references
- keep grouped outputs explainable and conservative

Constraints:
- do not create unsupported values
- do not overwrite extracted geometry or text
- prefer manual review when grouping is ambiguous
